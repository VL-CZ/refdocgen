@inherits ComponentBase

@code {
    [Parameter]
    public required string DocComment { get; set; }
}

@if (!string.IsNullOrEmpty(DocComment))
{
    <div class="mt-3">
        <div class="lead">Remarks</div>
        <i>@((MarkupString)DocComment)</i>
    </div>
}
