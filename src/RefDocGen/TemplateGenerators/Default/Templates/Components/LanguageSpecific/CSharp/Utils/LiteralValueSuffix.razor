@inherits ComponentBase

@code {
    [Parameter]
    public required LocalizedData<string>? Value { get; set; }
}

@if (Value is not null)
{
    <span> = </span>
    <span class="refdocgen-text-green">@Value[Language.CSharp]</span>
}
