@inherits ComponentBase

@code {
    [Parameter]
    public required string? DocComment { get; set; }
}

@if (DocComment is not null)
{
    <div class="my-3" data-id="example-section">
        <div class="lead">Example</div>
        <div class="text-body-secondary fst-italic" data-id="example-doc">
            @((MarkupString)DocComment)
        </div>
    </div>
}
