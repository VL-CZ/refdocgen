@using RefDocGen.TemplateProcessors.Shared.TemplateModels.Types
@inherits ComponentBase

@code {
    [Parameter]
    public required TypeParameterTM TypeParameter { get; set; }
}

@*
    A template rendering the constraints of a type parameter.
    Note that this template displays the constraints under the type declaration (such as in C#).

    If the constraints should be displayed within the type declaration (such as in F#), leave this component empty.
    Instead, render the constraints directly within the 'TypeDeclaration' component.
*@
