@using RefDocGen.TemplateProcessors.Shared.TemplateModels.Types
@inherits ComponentBase

@code {
    [Parameter]
    public required TypeParameterTM TypeParameter { get; set; }
}

@*
    A template rendering a type parameter.

    This component is intended to be displayed in the 'Type parameters' section within the declaration of a type or method.
    Optionally, it is possible to reuse this component in 'TypeDeclaration' and 'MethodDeclaration' components, to render the type parameters.
*@
