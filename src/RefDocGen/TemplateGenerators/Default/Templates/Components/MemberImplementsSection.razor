@inherits ComponentBase

@code {
    [Parameter]
    public required TypeLinkTM[] Interfaces { get; set; }
}

@if (Interfaces.Any())
{
    @foreach (var iface in Interfaces)
    {
        <div class="text-primary ms-4 mb-2" data-id="member-implements">
            <i>
                Implements
                <TypeLink Type="@iface" />
            </i>
        </div>
    }
}
