@inherits ComponentBase

@code {
    [Parameter]
    public required string? Value { get; set; }
}

@if (Value is not null)
{
    <span> = </span>
    <span class="refdocgen-text-green">@Value</span>
}
