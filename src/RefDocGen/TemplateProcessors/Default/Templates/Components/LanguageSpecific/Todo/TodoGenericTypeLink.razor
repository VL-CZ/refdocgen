@using RefDocGen.TemplateProcessors.Shared.TemplateModels.Links
@inherits ComponentBase

@code {
    [Parameter]
    public required GenericTypeLinkTM Type { get; set; }
}

@*
    A template rendering a link to a type (if the link is found), including links to its type parameters; see the 'GenericTypeLinkTM' class for further details.
    Optionally, it is possible to reuse this template in *Declaration templates to represent member types as well as parameter and return types.
*@
